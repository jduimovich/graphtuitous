apiVersion: apps/v1
kind: Deployment
metadata:
  name: rgb-service
  labels:
    app: rgb-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rgb-service
      version: v1
  template:
    metadata:
      labels:
        app: rgb-service
        version: v1
    spec:
      containers:
      - name: demoservice
        image: jduimovich/rgb:main
        resources:
          limits:
            cpu: "200m"
            memory: "64Mi"
          requests:
            cpu: "100m"
            memory: "64Mi"
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
